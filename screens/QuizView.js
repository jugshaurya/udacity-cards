import React from "react";
import { View, Text } from "react-native";

const QuizView = ({ card }) => {
  return (
    <View>
      <Text>QuizView</Text>
    </View>
  );
};

export default QuizView;
