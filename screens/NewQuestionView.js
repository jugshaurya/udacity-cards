import React from "react";
import { View, Text } from "react-native";

const NewQuestionView = ({ card }) => {
  return (
    <View>
      <Text>NewQuestionView</Text>
    </View>
  );
};

export default NewQuestionView;
