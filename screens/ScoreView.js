import React from "react";
import { View, Text } from "react-native";

const ScoreView = ({ card }) => {
  return (
    <View>
      <Text>ScoreView</Text>
    </View>
  );
};

export default ScoreView;
